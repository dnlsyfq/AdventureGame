


```

npm install readline-sync // while the game is running, we need special code that lets us ask questions and get answers. readline-sync is a tool that gives us this ability.

```


* import
```
const readline = require('readline-sync')

```


* git setup
```
.gitignore
node_modules/

git add .
git commit -m "Initial commit - character stats setup"
git remote add origin https://github.com/yourusername/AdventureGame.git
git push -u origin main
```
